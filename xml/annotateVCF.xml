<?xml version="1.0"?>
<tool id="annotateVCF" name="annotateVCF" version="1.0">
    <description>VCF annotation via samtools mpileup</description>
    <requirements>
        <requirement type="package" version="3.8.3">python</requirement>
        <requirement type="package" version="1.4.1">scipy</requirement>
    </requirements>
    <command><![CDATA[
    conda list;
    python --version;
    which python;
    python '$__tool_directory__/'annotateVCF.py $in_vcf $in_mpileup $out_vcf
    ]]></command>
    <inputs>
        <param argument="in_vcf" type="data" format="vcf" label="Input VCF to annotate"/>
        <param argument="in_mpileup" type="data" format="pileup" label="Samtools mpileup file corresponding to vcf"/>
    </inputs>
    <outputs>
        <!-- standard -->
        <data name="out_vcf" format="vcf" label="${tool.name} on ${on_string}: Result">
        </data>
    </outputs>
    <tests>
        <!-- #1 default -->
        <test expect_num_outputs="1">
            <param name="in_vcf" value="test1.vcf"/>
            <param name="in_mpileup" value="test.txt"/>
            <output name="out_vcf" file="test2.vcf"/>
        </test>
    </tests>
    <help><![CDATA[ annotateVCF is a tool written for the purpose of annotating VCF files generated by medaka or another variant caller and annotate those variants with allele frequency, depth of coverage, strand bias, etc
    ]]></help>
    <citations>
        <citation type="bibtex">@online{annotateVCF,
            author = {Nathan Roach},
            title = {annotateVCF},
            year = 2020,
            url = {https://github.com/NatPRoach/PublicGalaxyTools/},
            urldate = {2020-05-06}
        }</citation>
    </citations>
</tool>